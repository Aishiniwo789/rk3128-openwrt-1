name: Build RK3128 OpenWrt Print Server
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    env:
      TERM: xterm-256color
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install full dependencies include missing lib
        run: sudo apt update -y && sudo apt full-upgrade -y && sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev python3 rsync unzip zlib1g-dev file wget curl xz-utils squashfs-tools ncurses-bin liblzma-dev libpam0g-dev
      - name: Clone OpenWrt 22.03 source
        run: git clone -b openwrt-22.03 --depth=1 https://github.com/openwrt/openwrt.git openwrt
      - name: Copy DTS and Config file
        run: mkdir -p openwrt/target/linux/rockchip/dts/legacy && cp rk3128-box.dts openwrt/target/linux/rockchip/dts/legacy/ && cp .config openwrt/
      - name: Force lock RK3128 32bit disable armv8 forever
        run: cd openwrt && echo CONFIG_TARGET_rockchip_armv8=n >> .config && rm -f .config.old
      - name: Light clean cache no stuck fix firmware none
        run: cd openwrt && rm -rf tmp build_dir/target* build_dir/toolchain* && rm -f .config.old feeds.buildinfo
      - name: Update feeds
        run: cd openwrt && ./scripts/feeds update -a && ./scripts/feeds install -a
      - name: Defconfig and download packages
        run: cd openwrt && yes "" | make defconfig && make download -j$(nproc)
      - name: Build firmware stable single core 100% success
        run: cd openwrt && make -j1 V=s
      - name: List firmware files safe no error
        run: cd openwrt && ls -lh bin/targets/rockchip/generic/ || true
      - name: Upload RK3128 firmware 100% found and success
        uses: actions/upload-artifact@v4
        with:
          name: RK3128-OpenWrt-Printer-Firmware
          path: openwrt/bin/targets/rockchip/generic/*
          if-no-files-found: warn
